#include <iostream>
#include <string>

int main() {
    std::string input;

    // Read a line from standard input
    std::getline(std::cin, input);

    // Output the same line
    std::cout << input << std::endl;

    return 0;
}
