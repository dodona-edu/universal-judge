Aan de functie `splitsInWoorden` moet een string (`String`) doorgegeven worden.
De functie geeft een lijst van letters (`List<Character>`) terug.

```java
Submission.aFunctionCall("yes")
```

This is java.
